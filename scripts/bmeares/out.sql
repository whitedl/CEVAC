CREATE TABLE watt_temp_xref (
	[PointSliceID] DECIMAL(38, 0) NOT NULL, 
	[Room] VARCHAR(max) NOT NULL, 
	[RoomType] VARCHAR(max) NOT NULL, 
	[ObjecName] VARCHAR(max) NOT NULL, 
	[BLG] VARCHAR(max) NOT NULL, 
	[Floor] VARCHAR(max) NOT NULL, 
	[ReadingType] VARCHAR(max) NOT NULL, 
	[Alias] VARCHAR(max) NOT NULL
);
